# Changelog

## [1.2.2] - 2026-08-19
### Fixed
- 修复响应较慢的 WebDAV 目录在音乐库同步时触发 `context deadline exceeded` 的问题；同步扫描使用独立网络预算，并为任务推进请求预留完整处理窗口。

## [1.2.1] - 2026-08-19
### Fixed
- 修复部分 WebDAV 服务返回空目录显示名和规范化挂载路径时，文件浏览页只显示“目录”且点击后反复加载根目录的问题。

## [1.2.0] - 2026-08-19
### Added
- 新增 WebDAV 音乐库同步：可设置扫描根、递归发现音频，并按目录创建和幂等维护歌单。
- 新增可恢复同步任务：立即返回 task ID，提供阶段、进度、取消、重试，以及页面关闭、插件 VM 卸载或宿主重启后的 checkpoint 恢复。

### Changed
- WebDAV 配置、目录和歌曲改用稳定 ID 与规范化路径作为身份；配置改名、换挂载或受信任端点迁移后不再把整库识别为新来源。
- 扫描按单个 PROPFIND、Song 批次和歌单成员批次推进；写入前持久化 Song/歌单 intent 与临时 managed ownership，失败后可安全重试收敛。
- 发布构建直接锁定 JSC 2.13.0，确保最终插件使用 `main.jsc` 字节码入口。

### Fixed
- 严格校验 WebDAV 207 Multi-Status、propstat 状态、直接子资源与同源路径，发现不完整时不进入删除阶段。
- 修复部分添加后取消、bridge 写入成功但 checkpoint 失败、歌单创建后映射写入失败可能造成成员残留、重复歌单或用户元数据被重放覆盖的问题。
- 删除阶段发生非原子局部失败时明确返回 `failed_partial`，保留旧成功快照并允许重试收敛。

## [1.1.5] - 2026-08-12
### Fixed
- 连接测试在目录枚举成功后继续验证同源音乐文件的分段读取，避免 WebDAV 可浏览但实际播放返回 403 时误报“连接正常”。
- 对 WebDAV 文件重定向给出本机代理（native proxy）提示，并将无可测试音乐文件、服务端忽略 Range 或探测超时显示为“部分通过”。

## [1.1.3] - 2026-07-30
### Fixed
- 修复发布包可能同时残留 `main.js` / `main.jsc` 并导致 `entryHash`、`zipHash` 校验失败的问题；构建前清理中间目录，并增加最终 ZIP 契约校验。
- 恢复 npm 单锁文件的可复现安装，发布流程禁止覆写已存在的版本。

### Security
- 阻止异源 WebDAV `href`、封面地址和重定向携带认证信息，避免凭据跨主机外送。
- 修复配置页动态内容直接写入 HTML 和内联事件处理器导致的 DOM XSS 风险。

## [1.1.2] - 2026-07-28
### Added
- 支持将 WebDAV 音乐导入已有歌单。

## [1.1.1] - 2026-06-24
### Fixed
- 恢复 `package-lock.json`，确保 CI 中的 `npm ci` 可以正常执行。

## [1.1.0] - 2026-06-24
### Fixed
- 修复 XML 实体解码、PROPFIND URL 编码和浮动操作按钮样式问题。
